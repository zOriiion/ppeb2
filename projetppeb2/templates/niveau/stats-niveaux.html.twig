{% extends 'base.html.twig' %}

{% block title %}{{parent()}}Statistique des niveaux{% endblock %}
{% block body %}
{{parent()}}
<div class="container-fuid">
<div class="row justify-content-center">
<h1 class="text-center text-primary p-4"> Statistiques des tests par niveau</h1>
</div>
<div class="row justify-content-center">
        <div class="col-8 p-4 m-0 text-primary">
            <div class="table-responsive">
<table class="table table-hover">
    <thead>
    <tr>
        <th scope="col">Libelle du niveau</th>
        <th scope="col">Nombre de test étant de ce niveau</th>
    <tr>
    </thead>
    <tbody>
    {% for niveau in niveaux %}
        <tr class="{{ cycle(['table-primary', 'table-secondary'], loop.index0) }}">
        <td>{{niveau.libelle |capitalize }}</td>
        <td>{{niveau.NbTestParNiv |capitalize }}</td>
        
        </tr>
        {% endfor %}
 </tbody>
            </table>
</div></div></div></div>
{% endblock %}